'use client';

export default function WithdrawHistoryTab() {
  return (
    <div className="mx-auto flex w-full flex-col gap-4" style={{ maxWidth: 'var(--fin-table-width)' }}>
      <p className="text-muted-foreground">
        Tab de Histórico de Saques - Em desenvolvimento
      </p>
      <p className="text-sm text-muted-foreground">
        Este componente será implementado com a tabela de saques e filtros.
      </p>
    </div>
  );
}
