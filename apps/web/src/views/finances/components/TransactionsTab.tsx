'use client';

export default function TransactionsTab() {
  return (
    <div className="mx-auto flex w-full flex-col items-stretch gap-4" style={{ maxWidth: 'var(--fin-table-width)' }}>
      <p className="text-muted-foreground">
        Tab de Transações - Em desenvolvimento
      </p>
      <p className="text-sm text-muted-foreground">
        Este componente será implementado com a tabela de transações, filtros e paginação.
      </p>
    </div>
  );
}
